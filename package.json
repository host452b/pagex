{
  "name": "pagex",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "package": "node scripts/package-extension.mjs",
    "release": "npm run package && bash scripts/prepare-release.sh",
    "generate:store-assets": "node scripts/generate-store-assets.mjs"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "fflate": "^0.8.2",
    "jsdom": "^29.0.2"
  }
}
